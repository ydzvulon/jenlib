version: "3"

vars:
  _desc: |-
    Used only with include

tasks:

  build-docs:
    desc: build docs in current direction
    cmds:
      - mkdocs build

  serve-docs:
    desc: build docs in current direction
    cmds:
      - mkdocs build
